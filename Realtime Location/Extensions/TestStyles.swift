import SwiftUI

extension Font {
    static let heading = Font.system(size: 16)
    static let normal = Font.system(size: 12)
    static let medium = Font.system(size: 24)
    static let large = Font.system(size: 80)
}

extension Color {
    static let primary = Color.turquoise
    static let secondary = Color.azure
    static let background = Color.persianBlue
}
